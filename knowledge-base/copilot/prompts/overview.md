# Prompt Files Overview

- Prompt files are reusable `.prompt.md` templates with frontmatter (name, description, agent, tools, model).
- Reference tools inline with `#tool:<name>`; use `${variables}` for context and inputs.

Docs: https://code.visualstudio.com/docs/copilot/customization/prompt-files
