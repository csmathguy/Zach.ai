# MCP Servers Overview

- MCP servers expose tools/resources/prompts to Copilot via a standard protocol.
- Install via Extensions view (`@mcp`) or configure `mcp.json` (user/workspace). Review trust prompts.

Docs: https://code.visualstudio.com/docs/copilot/customization/mcp-servers
